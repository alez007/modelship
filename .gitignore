.venv
__pycache__
*.idea
.vscode
.claude
*.DS_Store
.cache
.build
local_models
models-cache
config/models.yaml
config/models_stack_*.yaml

# Helm: vendored subcharts are fetched via `helm dependency build` (Chart.lock
# is committed for reproducibility); never commit the downloaded artifacts.
helm/**/charts/
helm/**/*.tgz